using UnityEngine;

public class UpgrateButton : MonoBehaviour
{
    /*
    [SerializeField] private Game _game;
    [SerializeField] private int _price;
    [SerializeField] private int _upgrateValue;

    public void Upgrade()
    {
        if (_game._points >= _price)
        {
            _game._power += _upgrateValue;
            DecreasePoints(_price);
        }
    }

    private void DecreasePoints(int price)
    {
        _game._points -= price;
        _game.ShowPoints();
    }
    */
}